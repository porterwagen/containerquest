node_modules/
dist/
.next/
out/
*.tsbuildinfo

# Python
__pycache__/
*.py[cod]
.venv/

# Go
services/util/util

# C
services/compute/compute
*.o

# Generated by scripts/ — derived from real files, never hand-edited.
# recordings.json is the exception: it must be committed, because it holds real
# captured terminal output and the Vercel build has no Docker to re-record it.
apps/dashboard/src/generated/*
!apps/dashboard/src/generated/recordings.json

# Local env
.env
.env.local
.DS_Store
